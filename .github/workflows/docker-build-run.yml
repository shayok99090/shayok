name: Build and Run Docker Image

on:
  push:
    branches: [ main ]

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: |
         docker buildx shayok .
         docker run -d -p 3000:3000 shayok
